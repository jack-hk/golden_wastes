# Generated files by hugo
/public/
/resources/_gen/
/assets/jsconfig.json
hugo_stats.json
# Executable may be added to repository
hugo.exe
hugo.darwin
hugo.linux
# Temporary lock file while building
/.hugo_build.lock
# ------------
# Custom rules
# For local project-related content, such as wireframes, diagrams, etc
/local/
# Ignored because the theme (cleanwiki) has it's own repo, clone seperately inside this repo!
/themes/
# VSCode extensions
/node_modules/
