media:
  - name: images
    label: Uploaded images
    input: static/uploads
    output: /uploads
    categories: [image]
    extensions: [png, jpg, jpeg, gif, webp, svg]
    rename: safe
  - name: wiki_icons
    label: Wiki SVG icons
    input: static/SVG
    output: /SVG
    extensions: [svg]
    categories: [image]
    rename: safe

settings:
  content:
    merge: true
  commit:
    identity: user
    templates:
      create: "content: create {path}"
      update: "content: update {path}"
      delete: "content: delete {path}"
      rename: "content: rename {oldPath} to {newPath}"

components:
  infobox:
    type: object
    label: Infobox
    description: Optional structured side panel for wiki-layout pages, including imagery badges and labelled facts.
    required: false
    fields:
      - name: header
        label: Heading
        description: Title displayed at the top of the infobox.
        type: string
        required: false
        default: ""
      - name: image
        label: Image URL or repository path
        description: Main infobox image; use an uploaded /uploads path or an external URL.
        type: string
        required: false
        default: ""
      - name: icon
        label: Icon URL or /SVG path
        description: Fallback icon used when no infobox image is supplied.
        type: string
        required: false
        default: ""
      - name: imageSize
        label: Image size
        description: Infobox image width in pixels; zero or blank uses the site default.
        type: number
        required: false
        default: 0
      - name: iconSize
        label: Icon size
        description: Infobox icon width in pixels; zero or blank uses the site default.
        type: number
        required: false
        default: 0
      - name: imageMargin
        label: Image margin
        description: CSS margin around the infobox image, for example 10px auto.
        type: string
        required: false
        default: ""
      - name: iconMargin
        label: Icon margin
        description: CSS margin around the infobox icon, for example 10px auto.
        type: string
        required: false
        default: ""
      - name: iconColor
        label: Icon colour (CSS value)
        description: Colour applied to colourable SVG icons; accepts CSS colours or variables.
        type: string
        required: false
        default: ""
      - name: badgePath
        label: Badge URL or /SVG path
        description: Preferred image or SVG displayed as the infobox badge.
        type: string
        required: false
        default: ""
      - name: badge
        label: Badge path alias
        description: Legacy alternative to Badge path; use only for older content compatibility.
        type: string
        required: false
        default: ""
      - name: badgeIcon
        label: Badge icon alias
        description: Legacy badge-icon alternative; Badge path takes precedence.
        type: string
        required: false
        default: ""
      - name: badgeSvg
        label: Badge SVG alias
        description: Legacy badge-SVG alternative; Badge path takes precedence.
        type: string
        required: false
        default: ""
      - name: badgeSize
        label: Badge size
        description: Badge width and height in pixels.
        type: number
        required: false
        default: 0
      - name: badgeEnabled
        label: Show badge
        description: Displays the configured badge on the page infobox.
        type: boolean
        required: false
        default: false
      - name: badgeOnThumbnail
        label: Show badge on thumbnails
        description: Allows the badge to appear on cards or thumbnails for this entry.
        type: boolean
        required: false
        default: false
      - name: badgeBackgroundColor
        label: Badge background colour
        description: CSS colour behind the badge.
        type: string
        required: false
        default: ""
      - name: badgeColor
        label: Badge colour alias
        description: Legacy alternative for the badge background colour.
        type: string
        required: false
        default: ""
      - name: badgeForegroundColor
        label: Badge foreground colour
        description: CSS colour applied to the badge icon or foreground.
        type: string
        required: false
        default: ""
      - name: labels
        label: Infobox rows
        description: Ordered label/value rows shown inside the infobox.
        type: object
        list: true
        required: false
        default: []
        fields:
          - name: label
            label: Label
            description: Short heading for this infobox row, such as Type or Region.
            type: string
            required: false
          - name: item
            label: Value
            description: Row value; Markdown links and basic Markdown formatting are supported.
            type: string
            required: false
          - name: divider
            label: Show divider
            description: Replaces this row with a visual separator; label and value are ignored.
            type: boolean
            required: false

  references:
    type: object
    label: References
    description: External sources displayed in the page references section.
    list: true
    required: false
    default: []
    fields:
      - { name: title, label: Link title, type: string, description: Visible name of the source link. }
      - { name: url, label: URL, type: string, description: Complete destination URL for the source. }

content:
  - name: lore
    label: Lore
    type: collection
    path: content/lore
    exclude: [_index.md]
    format: yaml-frontmatter
    filename: "{primary}.md"
    actions:
      - name: move-file
        label: Move file
        workflow: pages-cms-move-file.yml
        ref: current
        scope: entry
        cancelable: false
        confirm:
          title: Move this file?
          message: Select the destination content folder. This only moves the file.
          button: Move file
        fields:
          - name: destination
            label: Destination folder
            type: select
            required: true
            options:
              - { label: Campaign, value: campaign }
              - { label: Categories, value: categories }
              - { label: Class, value: class }
              - { label: Event, value: event }
              - { label: Location, value: location }
              - { label: Lore, value: lore }
              - { label: Monster, value: monster }
              - { label: Placeholder, value: placeholder }
    fields:
      - { name: title, label: Title, type: string, description: Page heading and primary name used by lists search and the generated filename. }
      - name: date
        label: Published date
        description: Publication date used by Hugo for ordering metadata and future-page scheduling.
        type: date
        required: false
        options:
          time: true
          format: "yyyy-MM-dd'T'HH:mm:ssXXX"
      - { name: updated, label: Updated date, type: date, required: false, description: Last meaningful revision date shown in page metadata where enabled. }
      - { name: draft, label: Draft, type: boolean, required: false, default: false, description: Prevents the page from appearing in normal production builds. }
      - { name: placeholder, label: Hide as placeholder, type: boolean, required: false, default: false, description: Keeps template or unfinished content out of portals cards and public page output. }
      - { name: slug, label: URL slug, type: string, required: false, default: "", description: Replaces the filename-derived final URL segment without moving the source file. }
      - { name: url, label: URL override, type: string, required: false, default: "", description: Forces a complete site-relative URL; use carefully because it overrides the normal /lore path. }
      - { name: aliases, label: Previous URLs, type: string, list: true, required: false, default: [], description: Old site-relative URLs that Hugo should redirect to this page. }
      - { name: weight, label: Sort weight, type: number, required: false, default: 0, description: Manual ordering value; lower numbers sort before higher numbers where weighted ordering is used. }
      - { name: description, label: Description, type: string, required: false, default: "", description: Concise page description used by metadata search and components that request it. }
      - { name: summary, label: Summary, type: text, required: false, default: "", description: Custom excerpt for cards and lists; otherwise Hugo derives one from the body. }
      - { name: author, label: Author, type: string, required: false, default: "", description: Author credit displayed in page metadata when present. }
      - name: type
        label: Layout type
        description: Selects the page template; page is a standard article layout while wiki supports an infobox.
        type: select
        default: page
        options:
          values: [page, wiki]
      - name: categories
        label: Categories
        description: Taxonomy terms controlling category portals breadcrumbs cards and discovery; Lore posts should retain lore.
        type: string
        list: true
        default: [lore]
      - { name: tags, label: Tags, type: string, list: true, required: false, default: [], description: Additional taxonomy keywords used for navigation filtering and search context. }
      - { name: series, label: Series, type: string, required: false, default: "", description: Groups this page into a named sequence when series support is used. }
      - { name: image, label: Featured image, type: string, required: false, default: "", description: Thumbnail image used by cards lists and social or search presentation. }
      - { name: fitThumbnail, label: Fit image inside search thumbnail, type: boolean, required: false, default: false, description: Shows the complete featured image inside the square search-result thumbnail instead of cropping it to fill the frame. Overrides the globally disabled Pagefind thumbnail-fit setting for this page. }
      - { name: featured_image, label: Page header image, type: string, required: false, default: "", description: Large image rendered near the top of the page body. }
      - { name: image_caption, label: Image caption, type: string, required: false, default: "", description: Caption displayed beneath the page header image. }
      - { name: show_date, label: Show date, type: boolean, required: false, default: false, description: Shows the publication date in this page layout. }
      - { name: show_reading_time, label: Show reading time, type: boolean, required: false, default: false, description: Shows Hugo's estimated minutes to read beside the page metadata. }
      - { name: iconColor, label: Page icon colour, type: string, required: false, default: "", description: Default CSS colour for colourable SVG icons used by this page and its infobox. }
      - { name: badgeOnThumbnail, label: Show badge on thumbnails, type: boolean, required: false, default: false, description: Displays the configured infobox badge on this page's cards or thumbnails. }
      - { name: bookHidden, label: Hide from legacy book navigation, type: boolean, required: false, default: false, description: Legacy compatibility flag that removes the page from old book-style navigation. }
      - { name: BookToC, label: Show legacy book table of contents, type: boolean, required: false, default: false, description: Legacy compatibility flag for the old book table of contents. }
      - name: infobox
        component: infobox
        description: Structured side panel used by wiki-layout pages; standard page layouts may ignore it.
        default:
          header: ""
          image: ""
          icon: ""
          imageSize: 0
          iconSize: 0
          imageMargin: ""
          iconMargin: ""
          iconColor: ""
          badgePath: ""
          badge: ""
          badgeIcon: ""
          badgeSvg: ""
          badgeSize: 0
          badgeEnabled: false
          badgeOnThumbnail: false
          badgeBackgroundColor: ""
          badgeColor: ""
          badgeForegroundColor: ""
          labels: []
      - { name: also, label: Related pages, type: string, list: true, required: false, default: [], description: Page names used to build the wiki See also section. }
      - { name: references, component: references, default: [], description: Sources shown in the references footer. }
      - name: body
        label: Lore body
        type: rich-text
        description: 'Main Markdown content rendered on the page; supports formatting, shortcodes, and configurable embedded images. For image controls, switch to Source and put an attribute line immediately below the image, for example: {width="320" align="right" mobileMinWidth="180" mobileMaxWidth="320" caption="Optional caption"}. The Markdown image title is its hover tooltip.'
        options: { format: markdown, media: images, switcher: true }
    view:
      fields: [title, date, draft, type]
      primary: title
      sort: [title]
      search: [title, body]

  - name: campaigns
    label: Campaigns
    type: collection
    path: content/campaign
    format: yaml-frontmatter
    filename: "{primary}.md"
    actions:
      - name: move-file
        label: Move file
        workflow: pages-cms-move-file.yml
        ref: current
        scope: entry
        cancelable: false
        fields:
          - name: destination
            label: Destination folder
            type: select
            required: true
            options:
              - { label: Campaign, value: campaign }
              - { label: Categories, value: categories }
              - { label: Class, value: class }
              - { label: Event, value: event }
              - { label: Location, value: location }
              - { label: Lore, value: lore }
              - { label: Monster, value: monster }
              - { label: Placeholder, value: placeholder }
    fields:
      - { name: title, label: Title, type: string, description: Page heading and primary name used by lists search and the filename. }
      - name: date
        label: Published date
        description: Publication date used for metadata ordering and Hugo future-page scheduling.
        type: date
        required: false
        options:
          time: true
          format: "yyyy-MM-dd'T'HH:mm:ssXXX"
      - { name: updated, label: Updated date, type: date, required: false, description: Date of the latest meaningful content revision. }
      - { name: draft, label: Draft, type: boolean, required: false, description: Excludes the page from normal production builds. }
      - { name: placeholder, label: Hide as placeholder, type: boolean, required: false, description: Hides template or unfinished content from public portals and cards. }
      - { name: slug, label: URL slug, type: string, required: false, description: Replaces the filename-derived final URL segment. }
      - { name: url, label: URL override, type: string, required: false, description: Forces a complete site-relative URL instead of the normal /campaign path. }
      - { name: aliases, label: Previous URLs, type: string, list: true, required: false, description: Old URLs that Hugo redirects to this page. }
      - { name: weight, label: Sort weight, type: number, required: false, description: Manual ordering value; lower values appear first where weighting is used. }
      - { name: description, label: Description, type: string, required: false, description: Concise metadata and search description. }
      - { name: summary, label: Summary, type: text, required: false, description: Custom card and list excerpt; otherwise Hugo derives one from the body. }
      - { name: author, label: Author, type: string, required: false, description: Author credit displayed in page metadata when enabled. }
      - name: type
        label: Layout type
        description: Campaign entries use the standard page layout.
        type: select
        default: page
        options:
          values: [page]
      - name: categories
        label: Categories
        description: Controls taxonomy portals and breadcrumbs; Campaign entries should retain campaign.
        type: string
        list: true
        default: [campaign]
      - { name: tags, label: Tags, type: string, list: true, required: false, description: Additional taxonomy keywords used for navigation filtering and search context. }
      - { name: series, label: Series, type: string, required: false, description: Groups related campaign pages into a named sequence. }
      - { name: eventDate, label: Campaign date, type: string, required: false, description: In-world or scheduled campaign date displayed by campaign-aware components. }
      - { name: eventTime, label: Campaign time, type: string, required: false, description: In-world or scheduled campaign time displayed by campaign-aware components. }
      - { name: image, label: Featured image, type: string, required: false, description: Thumbnail used by campaign cards lists and search presentation. }
      - { name: fitThumbnail, label: Fit image inside search thumbnail, type: boolean, required: false, default: false, description: Shows the complete featured image inside the square search-result thumbnail instead of cropping it to fill the frame. Overrides the globally disabled Pagefind thumbnail-fit setting for this page. }
      - { name: featured_image, label: Page header image, type: string, required: false, description: Large image rendered near the top of the campaign page. }
      - { name: image_caption, label: Image caption, type: string, required: false, description: Caption displayed beneath the page header image. }
      - { name: show_reading_time, label: Show reading time, type: boolean, required: false, description: Displays Hugo's estimated minutes to read. }
      - { name: references, component: references, description: External sources displayed in the references footer. }
      - name: body
        label: Campaign body
        type: rich-text
        description: Main Markdown content rendered on the campaign page.
        options: { format: markdown, media: images, switcher: true }
    view:
      fields: [title, date, draft]
      primary: title
      sort: [title]
      search: [title, body]

  - name: monsters
    label: Monsters
    type: collection
    path: content/monster
    format: yaml-frontmatter
    filename: "{primary}.md"
    actions:
      - name: move-file
        label: Move file
        workflow: pages-cms-move-file.yml
        ref: current
        scope: entry
        cancelable: false
        fields:
          - name: destination
            label: Destination folder
            type: select
            required: true
            options:
              - { label: Campaign, value: campaign }
              - { label: Categories, value: categories }
              - { label: Class, value: class }
              - { label: Event, value: event }
              - { label: Location, value: location }
              - { label: Lore, value: lore }
              - { label: Monster, value: monster }
              - { label: Placeholder, value: placeholder }
    fields:
      - { name: title, label: Name, type: string }
      - { name: date, label: Published date, type: date, required: false }
      - { name: updated, label: Updated date, type: date, required: false }
      - { name: draft, label: Draft, type: boolean, required: false }
      - { name: placeholder, label: Hide as placeholder, type: boolean, required: false }
      - { name: slug, label: URL slug, type: string, required: false }
      - { name: url, label: URL override, type: string, required: false }
      - { name: aliases, label: Previous URLs, type: string, list: true, required: false }
      - { name: weight, label: Sort weight, type: number, required: false }
      - { name: description, label: Description, type: string, required: false }
      - { name: summary, label: Summary, type: text, required: false }
      - { name: author, label: Author, type: string, required: false }
      - name: type
        label: Layout type
        type: select
        options:
          values: [wiki, page]
      - name: categories
        label: Categories
        type: string
        list: true
      - { name: tags, label: Tags, type: string, list: true, required: false }
      - { name: series, label: Series, type: string, required: false }
      - { name: image, label: Featured image, type: string, required: false }
      - { name: fitThumbnail, label: Fit image inside search thumbnail, type: boolean, required: false, default: false, description: Shows the complete featured image inside the square search-result thumbnail instead of cropping it. Overrides the globally disabled setting for this page. }
      - { name: featured_image, label: Page header image, type: string, required: false }
      - { name: image_caption, label: Image caption, type: string, required: false }
      - { name: show_date, label: Show date, type: boolean, required: false }
      - { name: show_reading_time, label: Show reading time, type: boolean, required: false }
      - { name: iconColor, label: Page icon colour, type: string, required: false }
      - { name: badgeOnThumbnail, label: Show badge on thumbnails, type: boolean, required: false }
      - { name: bookHidden, label: Hide from legacy book navigation, type: boolean, required: false }
      - { name: BookToC, label: Show legacy book table of contents, type: boolean, required: false }
      - { name: infobox, component: infobox }
      - { name: also, label: Related pages, type: string, list: true, required: false }
      - { name: references, component: references }
      - name: body
        label: Monster body
        type: rich-text
        options: { format: markdown, media: images, switcher: true }
    view:
      fields: [title, type, infobox.header]
      primary: title
      sort: [title]
      search: [title, infobox.labels, body]

  - name: locations
    label: Locations
    type: collection
    path: content/location
    format: yaml-frontmatter
    filename: "{primary}.md"
    actions:
      - name: move-file
        label: Move file
        workflow: pages-cms-move-file.yml
        ref: current
        scope: entry
        cancelable: false
        fields:
          - name: destination
            label: Destination folder
            type: select
            required: true
            options:
              - { label: Campaign, value: campaign }
              - { label: Categories, value: categories }
              - { label: Class, value: class }
              - { label: Event, value: event }
              - { label: Location, value: location }
              - { label: Lore, value: lore }
              - { label: Monster, value: monster }
              - { label: Placeholder, value: placeholder }
    fields:
      - { name: title, label: Name, type: string }
      - { name: date, label: Published date, type: date, required: false }
      - { name: updated, label: Updated date, type: date, required: false }
      - { name: draft, label: Draft, type: boolean, required: false }
      - { name: placeholder, label: Hide as placeholder, type: boolean, required: false }
      - { name: slug, label: URL slug, type: string, required: false }
      - { name: url, label: URL override, type: string, required: false }
      - { name: aliases, label: Previous URLs, type: string, list: true, required: false }
      - { name: weight, label: Sort weight, type: number, required: false }
      - { name: description, label: Description, type: string, required: false }
      - { name: summary, label: Summary, type: text, required: false }
      - { name: author, label: Author, type: string, required: false }
      - name: type
        label: Layout type
        type: select
        options:
          values: [wiki, page]
      - name: categories
        label: Categories
        type: string
        list: true
      - { name: tags, label: Tags, type: string, list: true, required: false }
      - { name: series, label: Series, type: string, required: false }
      - { name: image, label: Featured image, type: string, required: false }
      - { name: fitThumbnail, label: Fit image inside search thumbnail, type: boolean, required: false, default: false, description: Shows the complete featured image inside the square search-result thumbnail instead of cropping it. Overrides the globally disabled setting for this page. }
      - { name: featured_image, label: Page header image, type: string, required: false }
      - { name: image_caption, label: Image caption, type: string, required: false }
      - { name: show_date, label: Show date, type: boolean, required: false }
      - { name: show_reading_time, label: Show reading time, type: boolean, required: false }
      - { name: iconColor, label: Page icon colour, type: string, required: false }
      - { name: badgeOnThumbnail, label: Show badge on thumbnails, type: boolean, required: false }
      - { name: bookHidden, label: Hide from legacy book navigation, type: boolean, required: false }
      - { name: BookToC, label: Show legacy book table of contents, type: boolean, required: false }
      - { name: infobox, component: infobox }
      - { name: also, label: Related pages, type: string, list: true, required: false }
      - { name: references, component: references }
      - name: body
        label: Location body
        type: rich-text
        options: { format: markdown, media: images, switcher: true }
    view:
      fields: [title, type, infobox.header]
      primary: title
      sort: [title]
      search: [title, infobox.labels, body]

  - name: classes
    label: Classes
    type: collection
    path: content/class
    format: yaml-frontmatter
    filename: "{primary}.md"
    actions:
      - name: move-file
        label: Move file
        workflow: pages-cms-move-file.yml
        ref: current
        scope: entry
        cancelable: false
        fields:
          - name: destination
            label: Destination folder
            type: select
            required: true
            options:
              - { label: Campaign, value: campaign }
              - { label: Categories, value: categories }
              - { label: Class, value: class }
              - { label: Event, value: event }
              - { label: Location, value: location }
              - { label: Lore, value: lore }
              - { label: Monster, value: monster }
              - { label: Placeholder, value: placeholder }
    fields:
      - { name: title, label: Class name, type: string }
      - { name: date, label: Published date, type: date, required: false }
      - { name: updated, label: Updated date, type: date, required: false }
      - { name: draft, label: Draft, type: boolean, required: false }
      - { name: placeholder, label: Hide as placeholder, type: boolean, required: false }
      - { name: slug, label: URL slug, type: string, required: false }
      - { name: url, label: URL override, type: string, required: false }
      - { name: aliases, label: Previous URLs, type: string, list: true, required: false }
      - { name: weight, label: Sort weight, type: number, required: false }
      - { name: description, label: Description, type: string, required: false }
      - { name: summary, label: Summary, type: text, required: false }
      - { name: author, label: Author, type: string, required: false }
      - { name: header, label: Page heading, type: string, required: false }
      - name: type
        label: Layout type
        type: select
        options:
          values: [wiki, page]
      - name: categories
        label: Categories
        type: string
        list: true
      - { name: tags, label: Tags, type: string, list: true, required: false }
      - { name: series, label: Series, type: string, required: false }
      - { name: image, label: Featured image, type: string, required: false }
      - { name: fitThumbnail, label: Fit image inside search thumbnail, type: boolean, required: false, default: false, description: Shows the complete featured image inside the square search-result thumbnail instead of cropping it. Overrides the globally disabled setting for this page. }
      - { name: featured_image, label: Page header image, type: string, required: false }
      - { name: image_caption, label: Image caption, type: string, required: false }
      - { name: show_date, label: Show date, type: boolean, required: false }
      - { name: show_reading_time, label: Show reading time, type: boolean, required: false }
      - { name: iconColor, label: Page icon colour, type: string, required: false }
      - { name: badgeOnThumbnail, label: Show badge on thumbnails, type: boolean, required: false }
      - { name: bookHidden, label: Hide from legacy book navigation, type: boolean, required: false }
      - { name: BookToC, label: Show legacy book table of contents, type: boolean, required: false }
      - { name: infobox, component: infobox }
      - { name: also, label: Related pages, type: string, list: true, required: false }
      - { name: references, component: references }
      - name: body
        label: Class body
        type: rich-text
        options: { format: markdown, media: images, switcher: true }
    view:
      fields: [title, header, type, infobox.header]
      primary: title
      sort: [title]
      search: [title, header, infobox.labels, body]

  - name: category_portals
    label: Category portals
    type: collection
    path: content/categories
    format: yaml-frontmatter
    subfolders: true
    filename: "{primary}.md"
    actions:
      - name: move-file
        label: Move file
        workflow: pages-cms-move-file.yml
        ref: current
        scope: entry
        cancelable: false
        fields:
          - name: destination
            label: Destination folder
            type: select
            required: true
            options:
              - { label: Campaign, value: campaign }
              - { label: Categories, value: categories }
              - { label: Class, value: class }
              - { label: Event, value: event }
              - { label: Location, value: location }
              - { label: Lore, value: lore }
              - { label: Monster, value: monster }
              - { label: Placeholder, value: placeholder }
    fields:
      - { name: title, label: Title, type: string }
      - { name: date, label: Date, type: date, required: false }
      - { name: updated, label: Updated date, type: date, required: false }
      - { name: draft, label: Draft, type: boolean, required: false }
      - { name: placeholder, label: Hide as placeholder, type: boolean, required: false }
      - { name: slug, label: URL slug, type: string, required: false }
      - { name: url, label: URL override, type: string, required: false }
      - { name: aliases, label: Previous URLs, type: string, list: true, required: false }
      - { name: weight, label: Sort weight, type: number, required: false }
      - { name: description, label: Description, type: string, required: false }
      - { name: summary, label: Summary, type: text, required: false }
      - { name: image, label: Portal image, type: string, required: false }
      - { name: fitThumbnail, label: Fit image inside search thumbnail, type: boolean, required: false, default: false, description: Shows the complete portal image inside the square search-result thumbnail instead of cropping it. Overrides the globally disabled setting for this page. }
      - name: portal_view
        label: Portal layout
        type: select
        options:
          values: [list, card]
      - { name: showcase_tiles, label: Showcase count, type: number, required: false }
      - { name: min_showcase_tiles, label: Minimum showcase count, type: number, required: false }
      - { name: card_count, label: Card count, type: number, required: false }
      - { name: min_card_count, label: Minimum card count, type: number, required: false }
      - { name: random_cards, label: Randomise cards, type: boolean, required: false }
      - { name: cards, label: Ordered card page paths, type: string, list: true, required: false }
      - { name: truncate_limit, label: Description limit, type: number, required: false }
      - { name: carousel_on_desktop, label: Desktop carousel, type: boolean, required: false }
      - { name: show_alphabet, label: Show alphabet, type: boolean, required: false }
      - { name: show_layout_toggle, label: Show layout toggle, type: boolean, required: false }
      - name: list_layout
        label: Default list layout
        type: select
        required: false
        options:
          values: [fixed, masonry]
      - { name: show_intro_text, label: Show intro text, type: boolean, required: false }
      - { name: intro_text, label: Intro text, type: string, required: false }
      - { name: show_left_textbox, label: Show left textbox, type: boolean, required: false }
      - { name: left_textbox_title, label: Left textbox title, type: string, required: false }
      - { name: left_textbox_text, label: Left textbox text, type: string, required: false }
      - { name: show_right_textbox, label: Show right textbox, type: boolean, required: false }
      - { name: right_textbox_title, label: Right textbox title, type: string, required: false }
      - { name: right_textbox_text, label: Right textbox text, type: string, required: false }
      - { name: show_bottom_textbox, label: Show bottom textbox, type: boolean, required: false }
      - { name: bottom_textbox_text, label: Bottom textbox text, type: string, required: false }
      - name: body
        label: Portal body
        type: rich-text
        options: { format: markdown, media: images, switcher: true }
    view:
      fields: [title, portal_view]
      primary: title
      sort: [title]
      search: [title]

  - name: home
    label: Home page
    type: file
    path: content/_index.md
    format: toml-frontmatter
    fields:
      - { name: title, label: Title, type: string }
      - name: date
        label: Date
        type: date
        required: false
        options:
          time: true
          format: "yyyy-MM-dd'T'HH:mm:ssXXX"
      - { name: draft, label: Draft, type: boolean, required: false }
      - { name: placeholder, label: Hide as placeholder, type: boolean, required: false }
      - { name: updated, label: Updated date, type: date, required: false }
      - { name: slug, label: URL slug, type: string, required: false }
      - { name: url, label: URL override, type: string, required: false }
      - { name: aliases, label: Previous URLs, type: string, list: true, required: false }
      - { name: weight, label: Sort weight, type: number, required: false }
      - { name: description, label: Description, type: string, required: false }
      - { name: summary, label: Summary, type: text, required: false }
      - { name: author, label: Author, type: string, required: false }
      - { name: categories, label: Categories, type: string, list: true, required: false }
      - { name: tags, label: Tags, type: string, list: true, required: false }
      - { name: image, label: Image, type: string, required: false }
      - { name: fitThumbnail, label: Fit image inside search thumbnail, type: boolean, required: false, default: false, description: Shows the complete image inside the square search-result thumbnail instead of cropping it. Overrides the globally disabled setting for this page. }
      - name: body
        label: Content
        type: rich-text
        options: { format: markdown, media: images, switcher: true }
